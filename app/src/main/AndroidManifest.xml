<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="space.lucydevine.gamecounter1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/menu1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CountTeleOp"
            android:label="@string/menu2" />
        <activity
            android:name=".ProgramDevice"
            android:label="@string/menu4" />
        <activity android:name=".EndMatchAndAdvance" android:label="@string/menu3"></activity>
    </application>

</manifest>